# Copy this file to .env (gitignored) and fill in real values.
# docker-compose reads .env automatically and substitutes these into
# docker-compose.yaml's environment: list for the eamerald container.

ENTRA_TENANT_ID=
ENTRA_CLIENT_ID=
ENTRA_CLIENT_SECRET=
